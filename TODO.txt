=== CRITICAL
- Use import maps on vite when bundling
- Add component actions

=== NICE TO HAVE
- Error with nice message when missing a DB library
- Add auth + user creation
- Add solid template
- Add vanilla template
- Add svelte template
- Add vue template
- Add postgres sql adapter
- Add mysql sql adapter
- Allow specifying entry point
- Handle errors on CLI when publishing the registry

=== AFTER MVP
- Allow multiple versions of a dependency

=== CUTE LITTLE THINGS
- Add message of when to do when creating registry through CLI
- Add JSON schema for json config style
- Improve performance on publishing (too much redundancy)
- Add CLI option to run registry from json
- Add "registry" option to component entry so CLI can read from it
- Bundle superjson on registry (make it an option)
- In dev mode floating window that lets you edit params